package section4;

public class MethodOverloadingChallenge {
	
	public static void main(String[] args) {
		
		calcFeetInchesToCentimeters(11, 2);
		calcFeetInchesToCentimeters(0, 0);
		calcFeetInchesToCentimeters(0, 2);
		calcFeetInchesToCentimeters(0, 14);
		
		System.out.println(calcFeetInchesToCentimeters(64));
		
	}
	
	public static double calcFeetInchesToCentimeters(int feet, int inches) {
		double result = 0;
		if (feet == 0 ) {
			if (inches >=0 && inches <=12) {
				result = (inches * 2.54);
				System.out.println(inches + " inches is " + result + " centimeters.");
				return result;
				
			} else {
				System.out.println("Please enter valid numbers. Inches cannot be greater than 12.");
			}
			
		} else if (feet >= 0 && inches <=12) {
			result = ( (feet * 12) + (inches) ) * 2.54;
			System.out.println(feet + " feet and " + inches + " inches is equal to " + result + " centimeters.");
			return result;
		}
		return result;
		
		
		
	}
	
	public static double calcFeetInchesToCentimeters(int inches) {
		double result = 0;
		
		if (inches >= 0) {
			result = inches * 2.54;
			System.out.println(inches + " inches is equal to " + result + " centimeters.");		
		} else {
			System.out.println("Please enter valid numbers.");
		}
		
		return result;
	}

}
